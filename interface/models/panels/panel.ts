/**
 * An interface created to differentiate panels from models
 * 
 * @description A panel is an all-in-one MVC class that is either a simple part of an interface
 * or a composition of more complex MVC components
 * 
 * @example An inventory includes a list with additional functionality, namely the ability to
 * switch between types of items, a weight limit, and the ability to 
 */
interface Panel {}